{
  "name": "@cursorrulecraft/backend",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "NODE_ENV=development nx dev backend",
    "build": "NODE_ENV=production nx build backend",
    "start": "NODE_ENV=production nx start backend",
    "start:prod": "NODE_ENV=production nx start backend",
    "lint": "nx lint backend",
    "type-check": "nx type-check backend"
  }
}
